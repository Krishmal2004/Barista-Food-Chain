services:
  node-backend:
    build: ./node-folder
    ports:
      - "3000:3000"
    environment:
      - ML_SERVICE_URL=http://ml-service:5000
    depends_on:
      - ml-service

  ml-service:
    build: ./ml-services
    ports:
      - "5000:5000"